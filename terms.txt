machine learning
model
training
inference
model architecture
parameters

linear model
coefficient, intercept
weights, biases
regression

neural network, layers
hidden layer
deep learning

supervised learning
training data
forward pass
independent variable, dependent variable
label
loss function
backward pass
stochastic gradient descent
fit
epoch

validation data
overfitting

pre-trained model
fine-tuning, alignment
reinforcement learning
Low-Rank Adaptation (LoRA)

tensor, matrix, array, ndarray
PyTorch, TensorFlow, Scikit-learn
accelerator, GPU, GPU kernel

language model, natural language processing (NLP)
large language model
token, subword encoding
prompts, prompt engineering, system prompt
zero-shot, one-shot, few-shot
transformers, GPT
attention mechanism
one-hot encoding
retrieval augmented generation (RAG)
embeddings, vector database

rabbit hole
